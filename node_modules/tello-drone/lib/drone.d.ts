/// <reference types="node" />
import dgram from "dgram";
import { EventEmitter } from "events";
import { ValidCommandOptions, ValidCommands } from "./types/commands.types";
import { DroneEventEmitter, DroneOptions } from "./types/drone.types";
declare class Drone {
    HOST: string;
    MAIN_PORT: number;
    STATE_PORT: number;
    droneIO: dgram.Socket;
    droneState: dgram.Socket;
    connected: boolean;
    events: EventEmitter;
    constructor(options?: DroneOptions);
    send(command: ValidCommands, options?: ValidCommandOptions, force?: boolean): Promise<void>;
    forceSend(command: ValidCommands, options: ValidCommandOptions): Promise<void>;
    on: DroneEventEmitter;
}
export = Drone;
